<h1>A review for restaurant <%= @restaurant.name %></h1>

<div class="review">
    <p>Rating: <%= "⭑" * @review.rating %></p>
    <p><%= @review.content %></p>
    <%= link_to "Go back", restaurant_path(@restaurant) %>
    <%= link_to "Edit review", edit_restaurant_review_path(@restaurant, @review) %>
    <%= link_to "🗑", restaurant_review_path(@restaurant, @review), method: "delete"%>
</div>