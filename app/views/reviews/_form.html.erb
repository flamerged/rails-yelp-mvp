  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :rating, as: :integer, collection: 0..5 %>
    <%= f.input :content, as: :text%>
    <%= f.submit class: "btn btn-primary" %>
  <% end %>
